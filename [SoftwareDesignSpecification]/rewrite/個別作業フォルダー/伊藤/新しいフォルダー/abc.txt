退会Action
クラスにimplements SessionAwareを付ける
int count =0;
count = dao.delete((String)session.get("user"));とする。
セッションは取り出したい値とのセットの方を指定するので

セッション系の名前をきちんとsessionとする。



そもそもsession.put("user_password",password)
という記述の仕方にするべきである。

ｊｓｐの方の#session.passwordは使わない。
これに合わせてログインの方でも変える。

追記password→emailにするemailはuniqueなため
UserWithdrawalActionでフィールドとセッション.getの後をemailとuseremailにする
ゲッターセッターも作っておく
DAOでpublic int delete(String email){とし"Delete from user WHERE email = ?";にしてps.setString(1, email);とし
UserApplyAction で		session.put("useremail", email);とする。

LoginACTIONでセッション名をsession.put("useremail", email);とする

ボタン関係についてインプッとにすれば中央に行く


userapplyで名前を二回送っているのがおかしい