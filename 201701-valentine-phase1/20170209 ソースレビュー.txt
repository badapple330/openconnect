

JavaDocのコメントを完成させる。
エラーは発生していないか？

=======>2017/02/09現在完成

-------------------------------------

@author は 横文字且つ大文字にする。
TAKUMA INOUEなど。
また、複数名が操作した場合は、@authorタグを増やして記述。

--------------------------------------------------------

@authorはinternousdevではなく、作成したひとを記載する。

------------------------------------------------

@returnの記述がおかしいものがある。
ERRORかERRORになっているなど。。

------------------------------------------------

@sinceはyyyy/mm/dd形式にする。
たとえば2017/2/09ではなく2017/02/09とする。

---------------------------------------------------

プログラムのフォーマットを統一する。
（{ }の場所や、各コードの開始位置など）

----------------------------------------------------

getter及びsetterに関するJavaDocの記載は例えば「familyNameの取得」ではなく「姓の取得」などとする。（familyNameの説明について、familyNameとするのはおかしい）
===========>終了
-----------------------------------------------------

package-info.javaを記載する。
該当するパッケージ群についての説明をおこなう。

------------------------------------------------------

JavaDocを生成して、漏れている記述、説明不足、言葉の統一ができていない、などはないか？

--------------------------------------------------------

System.out.println()は削除する。
======>終了

---------------------------------------------------------

不要なコメントアウト（プログラム中のゴミ）は消す。

======>終了

----------------------------------------------------------

ActionクラスにてDAOを使用する際は、
select系はboolean、List、Map、DTOなどで取得する。

insert系、update系、delete系は件数を取得する。
件数取得後、件数に従って処理を分岐すること。

=======>終了

------------------------------------------------------------

Actionクラスのリターン値は、最初に変数の初期化をおこなってから値を変化させる。
String ret=ERROR;など
→アルゴリズム上の漏れを防ぐ
=======>終了

-------------------------------------------------------------

シリアルバージョンUIDは、デフォルト（１L）でないものを生成する。
=======>終了

--------------------------------------------------------------

条件文にてすべてのケースが網羅されていないものがある。
→ifは書いてあるけれど、elseがないものなど。

========>終了

--------------------------------------------------------------

変数名などについて、キャメル型ではなくスネーク型になっているものがある。
また、変数名の命名規則が守られていないものがある。

=======>終了

----------------------------------------------------------------

returnの直前でString result=SUCCESSと宣言しているものがある。
データベースに問合せした結果などを考慮していない。

======>終了

----------------------------------------------------------------

Actionクラスのsetterとgetterは必ず対で宣言する。
（次期開発時やメンテナンス時の漏れを防ぐ）
==========>終了
----------------------------------------------------------------

DAOクラスのDBとの接続は、com.internousdev.util.DBConnectorを使用して、他のプロジェクトで用いられている記述を踏襲すること。（５つの引数を用いて接続する）
→メンテナンス性を保持する

===========> 完成

----------------------------------------------------------------

クレジットカードの判定は、一気に３カードに対してexecuteQueryを行わず、必要に応じておこなうようにする。（負荷分散。必要以上に接続をおこなわない。）
=======>しらない
----------------------------------------------------------------


使われていない変数は削除する。
======>終了


-----------------------------------------------------------------


@sinceが2016/11/04などになっているものがある。
（１１月？）

DTO	5件

======>終了

-----------------------------------------------------------------

共通するSQLファイルは、openconnectのリポジトリ内から実行すること。
SQLファイルをコピーして自身のプロジェクトフォルダに保管しないこと。
（増殖やオリジナルファイルが書き換わった際の思わぬバグを防ぐ）

=======>終了

-----------------------------------------------------------------

cssフォルダ内にpngファイルがある？
===========>終了

------------------------------------------------------------------

company_overviewはopenconnectのものを使用する。
（自身のプロジェクトでは作成しない）
その他footer.jspなど、openconnectのJSPを使用するものは、自身のプロジェクトに保管せずにopenconnectへリンクもしくはincludeすること。

=================>終了
---------------------------------------------------------------------

<br>の乱用はしない。<p>タグを活用すること。

2017/02/10
==============>多少はあるが修正<br><br>--><br>or<p>

----------------------------------------------------------------------

cssの宣言は特別な理由がない場合には、htmlファイルに記述せず、cssファイルとする

=============>終了
